engines:
  # Coding engine for programmer mode.
  default: claude_code
  claude_code:
    sdk: subprocess           # subprocess (current) | anthropic_agent (future SDK)
    model: claude-sonnet-4-6  # passed to claude --model; remove to use Claude Code's default
    flags: []                 # extra CLI flags
