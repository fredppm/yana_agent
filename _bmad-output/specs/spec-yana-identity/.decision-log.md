# Decision Log — SPEC-yana-identity

## 2026-06-14 — Create pass

**Source:** `brainstorming-session-2026-06-14-1600.md` (extension session, ideas #7–#26)

**Pass 1 — Coherence:**
All 7 capabilities have both intent and success. Intents describe WHAT (profile switcher, two-layer model, Graphiti-backed identity, First Breath flow, disposable workspaces, state-based launch, no-auth model) without prescribing HOW. Constraints all bend real decisions: no-auth rules out login screens; `owner::context` format rules out flat UUIDs; connector scoping rules out a global connectors.yaml. Three open questions remain where the brainstorming was explicitly exploratory rather than decided: single-profile UX, workspace deletion UX, and migration path for existing users. These are flagged in SPEC.md Open Questions.

**Pass 2 — Preservation:**
All 20 numbered ideas from the extension session were examined. Load-bearing ideas landed as follows:
- #7 (YANA as species) → Why section backdrop
- #8 (identity as living graph) → CAP-3 + Constraint on static files
- #10 (group_id as owner::context) → Constraints + architecture.md
- #12, #13, #15 (profile switcher, no-auth, Google model) → CAP-1 + architecture.md state diagram
- #16 (account = human + context) → CAP-2 + architecture.md two-layer model
- #17 (context as free slug) → Constraints
- #18 (disposable workspace) → CAP-7
- #19 (two layers owner/workspace) → CAP-2 + architecture.md
- #20 (workspace as connector scope) → Constraints + architecture.md connector scoping
- #21 (workspace as self-contained directory) → architecture.md
- #23 (single First Breath, two layers) → CAP-4
- #24 (additional workspace as light conversation) → CAP-5
- #25 (zero CLI) → CAP-6
- #26 (state as navigation) → CAP-6 success criterion + architecture.md state diagram
- #9 (UUID) → superseded by #10 (owner::context); logged as rejected path, not surfaced
- #11 (First Breath as civil registry) → absorbed into CAP-4 narrative
- #14 (two-level navigation) → absorbed into CAP-1 and architecture.md
- #22 (two First Breaths) → rejected during brainstorming itself; not surfaced

Wrapper-only content dropped: facilitation scaffolding, numbered idea labels, energy/novelty commentary.

**Verdict:** Spec passes both sweeps. Three open questions were raised and resolved immediately by the user (Fred, 2026-06-14):

1. **Single-profile UX** — No separate profile switcher screen. The TUI home is one unified screen: left/right navigates profiles, up/down navigates sessions of the active profile.
2. **Workspace deletion** — Only within the TUI. No file cleanup needed (no files in the new model).
3. **Migration path** — No runtime import. An optional out-of-band script may be written but is not committed or shipped as a product feature.

Spec updated to reflect all three decisions. No open questions remain. No invented content; all content is either explicit in the brainstorming or directly derivable from the existing codebase (PR #26 baseline, providers.yaml structure, Textual TUI surface).
